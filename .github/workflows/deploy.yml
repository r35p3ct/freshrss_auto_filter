name: Deploy AutoFilter Extension

on:
  push:
    branches:
      - master
    paths:
      - 'xExtension-AutoFilter/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync extension files to server
        run: |
          sudo rsync -av --no-g --no-p --delete \
            ./xExtension-AutoFilter/ \
            /srv/freshrss/extensions/xExtension-AutoFilter/

      - name: Set correct permissions
        run: |
          sudo chown -R www-data:www-data /srv/freshrss/extensions/xExtension-AutoFilter
          sudo chmod -R 755 /srv/freshrss/extensions/xExtension-AutoFilter

      - name: Restart FreshRSS container
        run: |
          sudo docker restart freshrss

      - name: Verify deployment
        run: |
          sleep 5
          docker logs freshrss --tail 20
